Here are your tasks, bro!

<ul>
  <% @tasks.each do |t| %>
    <div>
      <input type="checkbox" id="completed" name="completed" checked>
      <label for="completed"><%= link_to t.title, task_path(t) %> <%= link_to "🗑", delete_task_path(t), data: {turbo_method: :delete, turbo_confirm: "Are you sure?"}%></label>
</div>
  <% end %>
</ul>



<%= link_to "Add a new task", new_task_path%>


