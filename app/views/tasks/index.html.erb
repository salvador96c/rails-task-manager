<h1>Here are your tasks, bro!</h1>

<p></p>

<ul>
  <% @tasks.each do |t| %>
    <div class="list-group-list-group-flush-salvador">
      <% if t.completed %>
        <i class="fa-solid fa-square-check"></i>
      <% else %>
        <i class="fa-regular fa-square-check"></i>
      <% end %>

      <label for="completed"> <%= link_to t.title, task_path(t) %> <%= link_to "🗑", task_path(t), data: {turbo_method: :delete, turbo_confirm: "Are you sure?"}%></label>
    </div>
  <% end %>
</ul>



<%= link_to "Add a new task", new_task_path, class: "btn btn-salvador" %>
